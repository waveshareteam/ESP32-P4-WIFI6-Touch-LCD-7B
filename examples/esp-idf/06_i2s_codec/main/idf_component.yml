## IDF Component Manager Manifest File
dependencies:
  idf: ">=5.5,<7.0"
  waveshare/esp32_p4_wifi6_touch_lcd_7b:
    # The board BSP owns the ES8311/ES7210 codec and I2S wiring.
    version: "==3.0.1"
    rules:
      - if: "target in [esp32p4]"
